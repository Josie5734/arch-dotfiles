--global variables to be used across the config

monitors = {
    iiyama = "DP-3",
    benq = "HDMI-A-1",
    logik = "DP-2"
}

programs = {
    terminal = "kitty",
    fileManager = "kitty -e yazi", --opens a kitty window and runs yazi
    bar = "ashell",
    launcher = "fuzzel",
    browser = "firefox",
    idle = "hypridle"
}
